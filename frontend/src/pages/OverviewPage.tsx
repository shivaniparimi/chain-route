// Stub for Task 6: dashboard stats tiles + timeseries chart. This task's
// scope is only the routing/build scaffold, not real content.
export function OverviewPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold text-slate-900">Overview</h1>
      <p className="mt-2 text-slate-600">Dashboard stats and trends will render here.</p>
    </div>
  );
}
