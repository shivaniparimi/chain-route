// Stub for later tasks: the paginated/filterable payments table backed by
// GET /payments. This task's scope is only the routing/build scaffold,
// not real content.
export function PaymentExplorerPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold text-slate-900">Payments</h1>
      <p className="mt-2 text-slate-600">The payment explorer table will render here.</p>
    </div>
  );
}
